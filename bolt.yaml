services:
  - name: frontend
    project_path: .
    start_command: npm run dev
    type: web

  - name: backend
    project_path: backend
    start_command: python app.py
    type: api
    language: python
    proxy:
      path: /api
      target_port: 5000
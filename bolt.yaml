services:
  - name: frontend
    project_path: .
    start_command: npm run dev
    type: web

  - name: backend
    project_path: .
    start_command: python backend/app.py
    type: api
    proxy:
      path: /api
      target_port: 5000